import type { CSSProperties } from 'react';
interface Props {
    className?: string;
    color?: CSSProperties['color'];
    width?: number;
}
export declare const ProfileAvatar: ({ className, color, width, }: Props) => JSX.Element;
export {};
