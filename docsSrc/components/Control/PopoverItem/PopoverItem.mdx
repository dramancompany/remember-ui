---
name: PopoverItem
menu: Components/Control
---

import { Playground, Props } from 'docz';
import { PopoverItem } from './PopoverItem';
import { DoczContainer } from 'remember-ui';

# PopoverItem

## Properties

<Props of={PopoverItem} />

## Basic usage

<Playground>
  {() => {
    return (
      <DoczContainer direction="row">
        <PopoverItem onClick={() => alert("Pop!")}>
          쿠폰 등록
        </PopoverItem>
        <PopoverItem warning onClick={() => alert("Pop!")}>
          쿠폰 등록
        </PopoverItem>
        <PopoverItem bold onClick={() => alert("Pop!")}>
          쿠폰 등록
        </PopoverItem>
      </DoczContainer>
    );
  }}
</Playground>
