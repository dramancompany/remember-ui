---
name: Remember UI
route: /
---

import { Playground } from 'docz';
import { BaseButton } from './components/Button/BaseButton/BaseButton';
import { LinkButton } from './components/Button/LinkButton/LinkButton';
import { MoreButton } from './components/Button/MoreButton/MoreButton';
import { TopButton } from './components/Button/TopButton/TopButton'


import { BaseInput } from './components/Input/BaseInput/BaseInput'
import { DoubleInput } from './components/Input/DoubleInput/DoubleInput';
import { ImageInput } from './components/Input/ImageInput/ImageInput';
import { MaskingInput } from './components/Input/MaskingInput/MaskingInput';
import { Textarea } from './components/Input/Textarea/Textarea';


import { Tooltip } from './components/Tooltip/BaseTooltip/Tooltip';
import { InfoTooltip } from './components/Tooltip/InfoTooltip/InfoTooltip';


import { Checkbox } from './components/Control/Checkbox/Checkbox';
import { Radio } from './components/Control/Radio/Radio';
import { Select } from './components/Control/Select/Select';
import { Popover } from './components/Control/Popover/Popover';
import { PopoverItem } from './components/Control/PopoverItem/PopoverItem';

import { Chip } from './components/Chip/Chip.js';
import { Spinner } from './components/Spinner/Spinner';

import { Container } from './components/Container/Container';
import './index.scss';

# Remember UI

리멤버 UI 페이지입니다.

## Button Components

<Playground>
  Base Buttons
  <Container direction="row" className="container">
    <BaseButton>Default</BaseButton>
    <BaseButton fill>Fill</BaseButton>
    <BaseButton rounded={false} disabled>
      No Rounded
    </BaseButton>
    <BaseButton isLoading>isLoading</BaseButton>
    <BaseButton color="blue" size="large" width={150}>
      Large blue
    </BaseButton>
    <BaseButton fill color="plus" size="xlarge" width={150}>
      plus
    </BaseButton>
  </Container>

  Link Buttons
  <Container direction="row" className="container">
    <LinkButton>Default</LinkButton>
    <LinkButton showAddress={false}>Default</LinkButton>
    <LinkButton address="https://career.rememberapp.co.kr">Career</LinkButton>
    <LinkButton showAddress={false} address="https://career.rememberapp.co.kr">
      Career
    </LinkButton>
  </Container>

  More Buttons
  <Container direction="row" className="container">
    <BaseButton color="black">
      Show
      <MoreButton />
    </BaseButton>
    <BaseButton color="black">
      Hide
      <MoreButton value />
    </BaseButton>
    <BaseButton fill>
      Show
      <MoreButton size="large" />
    </BaseButton>
    <BaseButton fill>
      Hide
      <MoreButton value size="large" />
    </BaseButton>
  </Container>

  Top Buttons(스크롤 다운 후 하단 어디에선가...)
  <TopButton />

</Playground>


## Control Components

<Playground>
  {() => {
    const options = [
      {
        id: 1,
        label: '인재검색 50',
        value: '50',
      },
      {
        id: 2,
        label: '인재검색 100',
        value: '100',
      },
      {
        id: 3,
        label: '인재검색 200',
        value: '200',
      },
    ];
    const [current, setCurrent] = React.useState(options[0].id);
    return (
      <>
        Checkbox
        <Container direction="row" className="container">
          <Checkbox state="on" />
          <Checkbox state="off" />
          <Checkbox state="on" disabled />
        </Container>
        Radio
        <Container direction="row" className="container">
          <Radio isChecked />
          <Radio />
        </Container>
        Select
        <Container direction="row" className="container">
          <Select
            className="pass-type"
            label="상품 종류"
            value={current}
            onChange={setCurrent}
            options={options}
          />
        </Container>
        Popover
        <Container direction="row" className="container">
          <Popover
            content={
              <div>
                <PopoverItem onClick={() => alert("Pop!")}>
                  쿠폰 등록1
                </PopoverItem>
                <PopoverItem onClick={() => alert("Pop!")}>
                  쿠폰 등록2
                </PopoverItem>
                <PopoverItem onClick={() => alert("Pop!")}>
                  쿠폰 등록3
                </PopoverItem>
              </div>
            }
          >
          <BaseButton fill width={150} size={"large"}>Popover</BaseButton>
          </Popover>
        </Container>
      </>
    );
  }}
</Playground>


## Input Components

<Playground>
  Base Input
  <Container direction="row" className="container">
    <BaseInput
      name="baseInput"
      label="제목"
      placeholder="제목을 입력하세요 (최대 50자)"
      maxLength={50}
      marginBottom={10}
    />

    <BaseInput
      name="baseInputDisabled"
      label="제목"
      disabled
    />
  </Container>

  Double Input
  <Container direction="row" className="container">
    <DoubleInput
      label1="유효 기간"
      value1={12}
      value2={25}
      name1="expiryDateMonth"
      name2="expiryDateYear"
      placeholder1="MM"
      placeholder2="YY"
      maxLength={2}
      onlyNumber
    />
  </Container>

  Image Input
  <Container direction="row" className="container">
    <ImageInput
      label="회사 로고 이미지"
      id="company-logo-img"
    />
  </Container>

  Masking Input
  <Container direction="row" className="container">
    <MaskingInput
      mask={[
        /[0-9]/,
        /[0-9]/,
        /[0-9]/,
        '-',
        /[0-9]/,
        /[0-9]/,
        '-',
        /[0-9]/,
        /[0-9]/,
        /[0-9]/,
        /[0-9]/,
        /[0-9]/,
      ]}
      type="text"
      name="registraionNumber"
      label="사업자등록번호"
      placeholder="10자리 숫자 입력(000-00-00000)"
    />
  </Container>

   Textarea
  <Container direction="row" className="container">
    <Textarea
      label={"문의내용"}
      type="text"
      name="message"
      maxLength={1500}
      placeholder={"1500자 내로 문의 내용을 적어 주세요."}
      required
    />
  </Container>
</Playground>

## Tooltip Components

<Playground>
  Tooltip
  <Container direction="row" className="container">
    <Tooltip 
      content={
        <span>
          제안을 보낸 후보자는 포지션에서
          <br /> 삭제할 수 없습니다
        </span>
      }
    />
    <Tooltip
      offset="4, 20"
      arrow={false}
      theme="dcdark"
      size="medium"
      content="추가하기"
      placement="top"
      type="plus"
      iconStyle={{ marginLeft: '10px' }}
    />
    <Tooltip
      theme="dcdark"
      size="medium"
      content="전체 보기"
      placement="right"
      type="plus"
      customComponent={
        <div style={{marginLeft: '20px', cursor:'pointer'}}>
          전체보기
        </div>
      }
    />
  </Container>

  Info Tooltip
  <Container direction="row" className="container">
    <InfoTooltip 
      target="info1"
      content={
        <span>
          툴팁을 클릭하면, 다시 보지 않게 됩니다.<br /> 
          로컬스토리지에서 툴팁을 닫았는지 확인 후 보여줍니다.
        </span>
      }
    />
  </Container>

</Playground>

## General Components

<Playground>
  Chip
  <Container direction="row" className="container">
    <Chip>Some message1</Chip>
    <Chip>Some message2</Chip>
    <Chip>Some message3</Chip>
    <Chip>Some message4</Chip>
    <Chip isOverflowed>
      Blah blah~
      BlahblahBlahblahBlahblahBlahblahBlahblahBlahblahBlahblahBlahblahBlahblahBlahblahBlahblahBlahblahBlahblahBlahblahBlahblahBlahblah
    </Chip>
  </Container>

  Spinner
  <Container direction="row" className="container">
    <Spinner />
    <Spinner width={10} height={10} borderWeight={2} />
  </Container>

</Playground>
