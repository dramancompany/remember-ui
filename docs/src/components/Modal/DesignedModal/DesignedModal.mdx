---
name: DesignedModal
menu: Components/Modal
---

import { Playground, Props } from 'docz';
import { DesignedModal } from './DesignedModal';
import { Container } from '../../Container/Container';
import { BaseButton } from '../../Button/BaseButton/BaseButton';
import { Radio } from '../../Control/Radio/Radio';

# DesignedModal

## Properties

<Props of={DesignedModal} />

## Basic usage

<Playground>
  {() => {
    const [isOpen1, setIsOpen1] = React.useState(false);
    const [isProcessing, setIsProcessing] = React.useState(false);
    const [radioId, setRadioId] = React.useState(1);
    const setRadio = (id) => setRadioId(id);
    const handleSubmit = () =>{
      alert("보냈습니다")
    }
    return (
      <Container>
        <BaseButton fill width={150} size={"large"} onClick={()=>setIsOpen1(true)}>Open Designed Modal</BaseButton>
        <DesignedModal 
          isOpen={isOpen1}
          title={`구매`}
          subTitle={"이용권 구매하기"}
          submitText="확인"
          closeText="취소"
          submit={() => handleSubmit()}
          onClose={()=>setIsOpen1(false)}
          close={()=>setIsOpen1(false)}
          submitButtonDisabled={isProcessing}
          isLoading={isProcessing}
        >
          <div style={{display:"flex", justifyContent:"center", flexDirection:"column", alignItems:"center"}}>
            <div>Radio onClick Event Bind</div>
            <div>
              <Radio isChecked={radioId===1} onClick={() => setRadio(1)} /><span>옵션1</span> 
              <Radio isChecked={radioId===2} onClick={() => setRadio(2)} /><span>옵션2</span>
            </div>
            <br />
          </div>
        </DesignedModal>
      </Container>
    );
  }}
</Playground>
