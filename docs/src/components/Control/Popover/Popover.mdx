---
name: Popover
menu: Components
---

import { Playground, Props } from 'docz';
import { Popover } from './Popover';
import { PopoverItem } from '../PopoverItem/PopoverItem';
import { Container } from '../../Container/Container';
import { BaseButton } from '../../Button/BaseButton/BaseButton';

# Popover

## Properties

<Props of={Popover} />

## Basic usage

<Playground>
  {() => {
    const [isOpen, setIsOpen] = React.useState(false);
    return (
      <>
        <div style={{width:"150px"}}>
          <Popover
            content={
              <div>
                <PopoverItem onClick={() => alert("Pop!")}>
                  쿠폰 등록1
                </PopoverItem>
                <PopoverItem onClick={() => alert("Pop!")}>
                  쿠폰 등록2
                </PopoverItem>
                <PopoverItem onClick={() => alert("Pop!")}>
                  쿠폰 등록3
                </PopoverItem>
              </div>
            }
          >
          <BaseButton fill width={150} size={"large"} onClick={()=>setIsOpen(true)}>Open Popover</BaseButton>
          </Popover>
        </div>
      </>
    );
  }}
</Playground>
